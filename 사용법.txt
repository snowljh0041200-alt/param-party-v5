v41.1 CREATE FORCE MAIN FIX

수정:
- 모집글 등록 후 작성 화면에 남는 문제 수정
- 등록 완료 후 script + meta refresh로 메인 강제 이동
- 등록 버튼 중복 클릭 방지
- v41.0 보스 끝시간 기준 계산 유지
- 음성/실시간 갱신/디자인 유지

배포:
git add .
git commit -m "v41.1 create force main fix"
git push
