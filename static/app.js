let slotN=0;function qs(s){return document.querySelector(s)}function fmtTime(v){v=(v||'').replace(/[^0-9]/g,'').slice(0,4);return v.length>=3?v.slice(0,v.length-2)+':'+v.slice(v.length-2):v}function addSlot(){const job=qs('#slotJob')?.value;const box=qs('#slots');if(!job||!box)return;const div=document.createElement('div');div.className='slot';div.innerHTML=`<b>${job}</b><input type="hidden" name="slot_job_${slotN}" value="${job}"><button type="button" class="mini danger" onclick="this.parentElement.remove()">삭제</button>`;box.appendChild(div);slotN++}function updateCreateMode(){const cat=qs('#cat')?.value;document.querySelectorAll('.place').forEach(x=>x.style.display=(x.dataset.cat===cat)?'':'none');const slots=qs('#slotsBox');if(slots)slots.style.display=(cat==='사냥')?'':'none'}document.addEventListener('DOMContentLoaded',()=>{const cat=qs('#cat');if(cat){cat.addEventListener('change',updateCreateMode);updateCreateMode()}document.querySelectorAll("input[name='start_time'],input[name='end_time']").forEach(i=>{i.addEventListener('input',()=>{i.value=fmtTime(i.value)})})});